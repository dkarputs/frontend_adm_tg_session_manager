import React from "react";
import { Typography } from '@mui/material';

const AllUsers:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">AllUsers</Typography>
        </div>
    );
};

export default AllUsers;