import React from "react";
import { Typography } from "@mui/material";

const AccountChecker:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">AccountChecker</Typography>
        </div>
    );
};

export default AccountChecker;