import React from "react";
import { Typography } from '@mui/material';

const AllAccounts:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">AllAccounts</Typography>
        </div>
    );
};

export default AllAccounts;