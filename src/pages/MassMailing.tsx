import React from 'react';
import { Typography } from '@mui/material';

const MassMailing:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">MassMailing</Typography>
        </div>
    );
};

export default MassMailing;