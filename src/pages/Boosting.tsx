import React from "react";
import { Typography } from "@mui/material";

const Boosting:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">Накрутка</Typography>
        </div>
    );
};

export default Boosting;