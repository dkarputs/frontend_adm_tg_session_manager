import React from "react";
import { Typography } from "@mui/material";

const ParserChats:React.FC = () => {
    return (
        <div>
            <Typography variant="h4">ParserChats</Typography>
        </div>
    );
};

export default ParserChats;