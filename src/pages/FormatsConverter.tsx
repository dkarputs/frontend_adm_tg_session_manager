import React from "react";
import { Typography } from "@mui/material";

const FormatsConverter: React.FC = () => {
    return (
        <div>
            <Typography variant="h4">FormatsConverter</Typography>
        </div>
    );
};

export default FormatsConverter;